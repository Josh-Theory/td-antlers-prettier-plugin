import { CompletionItemKind, } from "vscode-languageserver-types";
import { exclusiveResult, } from "../antlers/tagManager.js";
export function getUniqueParameterArrayValuesSuggestions(paramAttribute, allValues) {
    const items = [], paramValues = paramAttribute.getArrayValue(), valuesToUse = allValues.filter((e) => !paramValues.includes(e));
    for (let i = 0; i < valuesToUse.length; i++) {
        items.push({
            label: valuesToUse[i],
            kind: CompletionItemKind.Variable,
        });
    }
    return exclusiveResult(items);
}
//# sourceMappingURL=uniqueParameterArraySuggestions.js.map