import { AntlersNode } from '../nodes/abstractNode.js';
import EnvironmentDetails from "../runtime/environmentDetails.js";
class NodeTypeAnalyzer {
    static analyze(nodes, environmentDetails) {
        nodes.forEach((node) => {
            if (node instanceof AntlersNode) {
                if (node.pathReference != null &&
                    node.pathReference.isStrictVariableReference) {
                    node.isTagNode = false;
                    return;
                }
                if (node.name != null) {
                    node.isTagNode = environmentDetails.isTag(node.name.name);
                }
            }
        });
    }
    static analyzeNode(node) {
        if (node.pathReference != null &&
            node.pathReference.isStrictVariableReference) {
            node.isTagNode = false;
            return;
        }
        if (NodeTypeAnalyzer.environmentDetails != null && node.name != null) {
            node.isTagNode = NodeTypeAnalyzer.environmentDetails.isTag(node.name.name);
        }
        if (node.name != null) {
            node.isConditionNode = EnvironmentDetails.alwaysLikeTag.includes(node.name.name);
        }
    }
    static analyzeParametersForModifiers(node) {
        node.parameters.forEach((parameter) => {
            if (NodeTypeAnalyzer.environmentDetails != null) {
                parameter.isModifierParameter =
                    NodeTypeAnalyzer.environmentDetails.isModifier(parameter.name);
            }
        });
    }
}
NodeTypeAnalyzer.environmentDetails = null;
if (typeof NodeTypeAnalyzer.environmentDetails == "undefined" ||
    NodeTypeAnalyzer.environmentDetails == null) {
    NodeTypeAnalyzer.environmentDetails = new EnvironmentDetails();
}
export default NodeTypeAnalyzer;
//# sourceMappingURL=nodeTypeAnalyzer.js.map