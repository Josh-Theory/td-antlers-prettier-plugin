import { makeTagDocWithCodeSample } from '../../../documentation/utils.js';
const Children = {
    tagName: 'children',
    hideFromCompletions: false,
    requiresClose: true,
    injectParentScope: false,
    allowsContentClose: false,
    allowsArbitraryParameters: false,
    parameters: [],
    introducedIn: null,
    resolveDocumentation: (params) => {
        return makeTagDocWithCodeSample('Children Tag', 'The `children` tag fetches entries that are descendants of the current entry in a structured collection, such as pages.', `{{ children }}
    <a href="{{ url }}">{{ title }}</a>
{{ /children }}`, 'https://statamic.dev/tags/children');
    }
};
export default Children;
//# sourceMappingURL=children.js.map