export function blueprintFieldToScopeVariable(symbol, field) {
    return {
        sourceName: field.blueprintName,
        dataType: field.type,
        name: field.name,
        sourceField: field,
        introducedBy: symbol
    };
}
export function valuesToDataMap(variables) {
    const mapToReturn = new Map();
    for (let i = 0; i < variables.length; i++) {
        mapToReturn.set(variables[i].name, variables[i]);
    }
    return mapToReturn;
}
export function blueprintFieldsToScopeVariables(symbol, blueprintFields) {
    const variables = [];
    for (let i = 0; i < blueprintFields.length; i++) {
        const field = blueprintFields[i];
        variables.push({
            sourceName: field.blueprintName,
            dataType: field.type,
            name: field.name,
            sourceField: field,
            introducedBy: symbol
        });
    }
    return variables;
}
//# sourceMappingURL=scopeUtilities.js.map