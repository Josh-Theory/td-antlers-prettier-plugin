export function makeTagDocWithCodeSample(title, description, code, docLink) {
    let docs = '**' + title + '**  ';
    docs += "\n";
    docs += description + '  ';
    docs += "\n```antlers\n";
    docs += code;
    docs += "\n```";
    if (docLink != null) {
        docs += "\n\n";
        docs += '[Documentation Reference](' + docLink + ')';
    }
    return docs;
}
export function makeTagDoc(title, description, docLink) {
    let docs = '**' + title + '**  ';
    docs += "\n";
    docs += description + '  ';
    if (docLink != null) {
        docs += "\n\n";
        docs += '[Documentation Reference](' + docLink + ')';
    }
    return docs;
}
//# sourceMappingURL=utils.js.map